{
  "name": "setup-zig",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build main.js -o dist --source-map && ncc build post.js -o dist/post --source-map",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/cache": "^5.0.5",
    "@actions/core": "^2.0.3",
    "@actions/exec": "^2.0.0",
    "@actions/github": "^8.0.1",
    "@actions/tool-cache": "^4.0.0",
    "axios": "^1.8.2",
    "sodium-javascript": "^0.8.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
